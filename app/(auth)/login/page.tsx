// Substituirá o login.html demo do protótipo — autenticação real via Supabase Auth
// entra na fase do schema/auth (ver docs/04 e docs/06 no repo do protótipo).
export default function LoginPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center px-6 py-24">
      <h1 className="text-3xl font-extrabold tracking-[-0.03em]">Entrar</h1>
      <p className="mt-3 text-muted">Login — em construção (Supabase Auth).</p>
    </main>
  );
}
